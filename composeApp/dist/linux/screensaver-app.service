[Unit]
Description=Screen Saver App daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/screensaver-app --daemon
Restart=on-failure

[Install]
WantedBy=graphical-session.target
